<?php

// Настройки маскота-рыбы (см. App\Service\FishFoodService): суммы корма за
// действия и пороги накопленного fish_total_fed для перехода на следующий
// уровень. Всё в одном месте, а не разбросано по коду начисления.

return [
    // За сдачу домашки нет отдельного флэта и нет разового бонуса за первую
    // домашку в жизни — единственная награда за сам факт завершения работы —
    // homework_on_time_bonus, и только если сдано не позже due_at (см.
    // FishFoodService::awardHomeworkCompletion()); просрочка не даёт ничего
    // сверх баллов за задания. Корм за отдельное задание (в визарде и в
    // проверке куратором) равен баллу, полученному за задание (см.
    // FishFoodService::syncTaskCorm()), поэтому здесь нет отдельной суммы
    // вроде 'correct_answer'.
    'corm' => [
        'homework_on_time_bonus' => 5,
        'daily_visit'            => 3,
    ],

    // Накопленный fish_total_fed для перехода на уровень 2 / 3 / 4 (Икринка →
    // Маленькая акула → Большая акула → Акула качок). Дальше пороги растут
    // заметно круче — долгая цель на "Акулу качка".
    'levels' => [40, 120, 400],

    // Слаг персонажа на уровень — файлы лежат в
    // public/img/mascot/level-{N}/{slug}_{state}.png (состояния: default,
    // correct, partly_correct, wrong, eating — см. FishFoodService::mascotImageUrl()).
    // Уровень без записи здесь (или без файлов на диске) отрисуется старой
    // SVG-заглушкой fish-level-{N}.svg, пока не пришлют арт.
    'characters' => [
        1 => 'ikrinka',
        2 => 'little_shark',
        3 => 'grown_shark',
        4 => 'gym_shark',
    ],

    // Названия уровней для UI (карточка кормления, баннер левел-апа).
    'level_names' => ['Икринка', 'Маленькая акула', 'Большая акула', 'Акула качок'],

    // Фон за маскотом — файлы в public/img/mascot/background/{slug}.jpg.
    // Список доступных для выбора в профиле — App\Service\FishFoodService::
    // availableBackgrounds() сканирует папку на диске, здесь только подписи
    // для уже известных слагов; для файла без подписи возьмётся сам слаг.
    'default_background' => 'pure_ocean',
    'background_labels' => [
        'pure_ocean'      => 'Чистый океан',
        'ocean_bottom'    => 'Дно океана',
        'atlantida'       => 'Атлантида',
        'crystals'        => 'Пещера с кристаллами',
        'pirates'         => 'Затонувший корабль',
        'gym'             => 'Спортзал',
        'library'         => 'Библиотека',
        'hotel'           => 'Люкс-номер отеля',
        'happy_new_year'  => 'Новый год',
    ],

    // Бесплатные фоны — доступны всем сразу, без покупки. Остальные нужно
    // открывать за корм (см. background_prices) — см. FishFoodService::
    // isBackgroundUnlocked()/unlockedBackgroundsFor(). Явный список, а не
    // "первые N" по сортировке — так порядок в availableBackgrounds() можно
    // менять, не задевая, что именно бесплатно.
    'free_backgrounds' => ['pure_ocean', 'ocean_bottom'],

    // Цена по тирам, а не одна на все — иначе всего 7 платных фонов по 10
    // корма (70 суммарно) кончаются за первую-вторую неделю активного
    // ученика, и весь корм за остаток года просто лежит без дела. Слаг без
    // записи здесь (новый фон, ещё не оценили) — see FishFoodService::
    // backgroundPrice(), фолбэк на цену обычного тира (20).
    'background_prices' => [
        'pirates'        => 40,
        'atlantida'      => 40,
        'library'        => 40,
        'gym'            => 60,
        'crystals'       => 80,
        'hotel'          => 120,
        'happy_new_year' => 70, // сейчас скрыт (см. seasonal_backgrounds), цена не актуальна
    ],

    // Сезонные фоны — показываются в выборе только в перечисленные месяцы
    // (1-12), остальное время скрыты из FishFoodService::availableBackgrounds()
    // целиком (не просто "заблокированы", а не видны вообще), даже если
    // файл уже лежит на диске. "Новый год" — только декабрь.
    'seasonal_backgrounds' => [
        'happy_new_year' => [12],
    ],

    // Вторая косметическая категория — аксессуары рыбы (шляпа, очки и
    // т.п.), тем же принципом, что и фоны: бесплатный дефолт "без
    // аксессуара" + платные по тирам. Реального арта нет — вместо картинки
    // эмодзи-плейсхолдер (см. FishFoodService::accessoryEmoji()), заменить
    // на арт можно будет прямо здесь, без миграций и правок контроллера.
    'accessories' => [
        'none'    => ['label' => 'Без аксессуара', 'emoji' => '🐟', 'price' => 0],
        'bow'     => ['label' => 'Бантик',         'emoji' => '🎀', 'price' => 15], // обычный
        'glasses' => ['label' => 'Очки',           'emoji' => '🕶️', 'price' => 15],
        'bandana' => ['label' => 'Бандана',        'emoji' => '🧣', 'price' => 15],
        'flower'  => ['label' => 'Цветок',         'emoji' => '🌸', 'price' => 15],
        'hat'     => ['label' => 'Шляпа',          'emoji' => '🎩', 'price' => 30], // особый
        'medal'   => ['label' => 'Медаль',         'emoji' => '🏅', 'price' => 30],
        'crown'   => ['label' => 'Корона',         'emoji' => '👑', 'price' => 60], // редкий
    ],

    // 'none' (без аксессуара) доступен всем сразу и выбран по умолчанию.
    'free_accessories' => ['none'],
];
