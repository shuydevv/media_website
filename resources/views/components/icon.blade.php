{{--
    <x-icon name="lock-01" class="w-5 h-5 text-zinc-400" /> — единая точка входа
    для иконок из набора Untitled UI (см. resources/views/components/icon/*.blade.php,
    каждый файл — одна иконка "как есть" с сайта, без правок содержимого).

    Раньше каждая иконка была вписана инлайн-SVG прямо в разметку, одни и те
    же глифы (например "фото отсутствует") дублировались в 5+ файлах — любая
    правка/замена требовала находить и чинить все копии. Теперь замена одной
    иконки = правка одного файла в components/icon/.

    Все SVG используют stroke="currentColor" — cvет наследуется от text-*
    класса на самом <x-icon>, размер — от width/height (по умолчанию 24,
    можно переопределить пропом) или w-*/h*- классов.
--}}
@props(['name'])

<x-dynamic-component :component="'icon.' . $name" {{ $attributes }} />
